# Compose env override — host-specific (WINDOWS_HOST, model picks).
# Copy `.env.example` → `.env` and edit. The example file is tracked;
# `.env` itself never is.
.env
.env.local
.env.*.local

# Python bytecode caches (agent/ and its experiments/ generate these on run).
__pycache__/
*.py[cod]
